##############################################################################################
###1.测试功能点：bool查询
##
#
###2.测试项：(每新添一测试项，需在添加前注明测试项，注释填写在新添sq1前，不填在此处，如下样例）
###3.问题网址：xxx
###4.问题号：xxx
###5.预计运行时间：xxx
###6.编写/修改人：zhuyinying
###7.编写/修改日期：2019-12-23
###8.其他（选填）：case格式规范化修改
###
##
#
##############################################################################################

#####################################Do sql###################################################

--disable_warnings
drop table if exists a1;
--enable_warnings

###2-1:在range分区规则下，测试bool查询

create table a1(rowkey_suffix int primary key,price int) partition by range(rowkey_suffix) (partition z1 values less than (2),partition z2 values less than (5),partition z3 values less than maxvalue);
--real_sleep 1
insert into a1(rowkey_suffix,price) values(0,0),(1,1),(2,2);
--real_sleep 2
select * from a1 where not(price and 1);
select * from a1 where not(1 and price);
select * from a1 where not(price and 1)=0;
select * from a1 where not(1 and price)=0;
select * from a1 where not(1 and price)=1;
select * from a1 where not(1 and price)=1 is null;

select * from a1 where not price between 2 and 3;
select * from a1 where not price between 2 and 3;
select rowkey_suffix,price is false,price is true,price is unknown from a1;
select rowkey_suffix,price is not false,price is not true,price is not unknown from a1;

drop table a1;

--disable_warnings
drop table if exists yao_new_sql_test;
--enable_warnings
create table yao_new_sql_test(c0 varchar(20),c1 int primary key) partition by list(c1) (partition w1 values in(1,2),partition w2 values in (3,4));
--real_sleep 1
insert into yao_new_sql_test(c0,c1) values('hello',3);
insert into yao_new_sql_test(c0,c1) values('hello',1);
--real_sleep 1
select * from yao_new_sql_test where c1= false;
drop table yao_new_sql_test;


###///新添测试项///###
###2-x:(新增测试项说明)
#do sql
########################################################################################################
