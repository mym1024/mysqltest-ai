####################################必填######################################################
###1.测试功能点：由于两次分区导致主ups丢失
##
#
###2.测试项：(每新添一测试项，需在添加前注明测试项，注释填写在新添sq1前，不填在此处，如下样例）
###3.问题网址：xxx
###4.问题号：xxx
###5.预计运行时间：11.59 s
###6.编写/修改人：pantong
###7.编写/修改日期：2019-11-5
###8.其他（选填）：新编case
###
##
#
##############################################################################################

#####################################Do sql###################################################
--disable_warnings
drop table if exists t1,t2,t3,t4,t5;
--enable_warnings


###2-1：由于两次分区导致主ups丢失
--disable_abort_on_error
--disable_warnings
drop table if exists t1,t2;
--enable_warnings

--error 5077
drop partition function e;
create enum partition function e(x) '[(1)],[(3)]';

create table t1(pk int primary key,a int)partition by(em,e,pk);
insert into t1 values(1,0),(2,0),(0,0);

if($NEW_MASTER_UPS_IP == None)
{
	--echo two update servers are required
	--exit
}
exec $LOCAL_DIR/tools/as_admin -r $RS0_IP -p $RS0_PORT change_ups_master -o ups_ip=$NEW_MASTER_UPS_IP -o ups_port=$NEW_MASTER_UPS_PORT > /dev/null 2>&1;
exec $LOCAL_DIR/tools/as_admin -r $RS0_IP -p $RS0_PORT change_ups_master -o ups_ip=$MASTER_UPS_IP -o ups_port=$MASTER_UPS_PORT > /dev/null 2>&1;

--real_sleep 10
insert into t1 values(3,0),(5,0),(4,0);
insert into t1 values(1,0),(2,0),(0,0);

drop table t1;

###///新添测试项///###
###2-x:(新增测试项说明)
#do sql
########################################################################################################
