--source merge.sql
##############################################################################################
###1.测试功能点：一张数据表中，对其创建的索引表数量的限制
##
#
###2.测试项：(每新添一测试项，需在添加前注明测试项，注释填写在新添sq1前，不填在此处，如下样例）
###3.问题网址：xxx
###4.问题号：xxx
###5.预计运行时间：1313s
###6.编写/修改人：zhuyinying
###7.编写/修改日期：2019-10-10
###8.其他（选填）：case格式规范化修改
###
##
#
##############################################################################################

#####################################Do sql###################################################
alter system set balance_max_concurrent_migrate_num=30 server_type=yaoadminsvr;
alter system set balance_max_migrate_in_per_ds=6 server_type=yaoadminsvr;
alter system set balance_max_migrate_out_per_ds=6 server_type=yaoadminsvr;
alter system set max_migrate_task_count=6 server_type=yaodatasvr;

--disable_warnings
drop table if exists t1;
--enable_warnings

--disable_query_log
--enable_query_log

create table t1(c1 int primary key,c2 int);

###2-1:在一张表中创建9个索引表，未超限
create index il0 on t1(c1) storing (c2);
create index il1 on t1(c1) storing (c2);
create index il2 on t1(c1) storing (c2);
create index il3 on t1(c1) storing (c2);
create index il4 on t1(c1) storing (c2);
create index il5 on t1(c1) storing (c2);
create index il6 on t1(c1) storing (c2);
create index il7 on t1(c1) storing (c2);
create index il8 on t1(c1) storing (c2);

###2-2:在一张表中创建10个索引表，未超限
create index il9 on t1(c1) storing (c2);

###2-3:在一张表中创建11个索引表，超限
#--error 65535
create index i20 on t1(c1) storing (c2);
show index on t1;
drop table t1;
select table_id,table_name,table_type from __first_tablet_entry;

###///新添测试项///###
###2-x:(新增测试项说明)
#do sql
########################################################################################################
