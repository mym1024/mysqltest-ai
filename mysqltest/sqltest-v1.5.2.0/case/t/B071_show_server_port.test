##############################################################################################
###1.测试功能点:xxx
##                  
#                                     
###2.测试项：(每新添一测试项，需在添加前注明测试项，注释填写在新添sq1前，不填在此处，如下样例）
###3.问题网址：
###4.问题号：
###5.预计运行时间：xxx
###6.编写/修改人： zhuyinying
###7.编写/修改日期：2020-1-14
###8.其他（选填）：case格式规范化修改
###
##
#
##############################################################################################

#####################################Do sql###################################################

let $rs1port = query_get_value(show parameters where server_type='yaoadminsvr' and name='port',server_port,1);
let $up1sport = query_get_value(show parameters where server_type='yaotxnsvr' and name='port',server_port,1);
let $cs1port = query_get_value(show parameters where server_type='yaodatasvr' and name='port',server_port,1);
let $ms1port = query_get_value(show parameters where server_type='yaosqlsvr' and name='port',server_port,1);

let $rs2port = query_get_value(select svr_port from (select * from __all_server) tt where tt.svr_type='yaoadminsvr',svr_port,1);
let $ups2port = query_get_value(select svr_port from (select * from __all_server) tt where tt.svr_type='yaotxnsvr',svr_port,1);
let $cs2port = query_get_value(select svr_port from (select * from __all_server) tt where tt.svr_type='yaodatasvr',svr_port,1);
let $ms2port = query_get_value(select svr_port from (select * from __all_server) tt where tt.svr_type='yaosqlsvr',svr_port,1);


if($rs1port == $rs2port)
{
   --echo rsports are identical
}

if($up1sport == $ups2port)
{
   --echo upsports are identical
}

if($cs1port == $cs2port)
{
   --echo csports are identical
}

if($ms1port != $ms2port)
{
   --echo msports are unidentical
}


###///新添测试项///###
###2-x:(新增测试项说明)
#do sql
########################################################################################################
